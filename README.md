# sacredheartoakford.github.io

## Image Compression

This project includes a script to convert JPG images to AVIF format for better compression.

### Usage

```bash
python src/pages/image_compress.py <folder_path>
```

The script will convert all JPG images in the specified folder to AVIF format and save them in the same location.